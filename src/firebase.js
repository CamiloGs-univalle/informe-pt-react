/**
 * src/firebase.js — MOVED
 * ─────────────────────────────────────────────────────────────────────────
 * Relocated to `src/config/firebase.js` (see docs/ARCHITECTURE.md and
 * CHANGELOG.md). Kept as a re-export for backwards compatibility; safe to
 * delete once confirmed unused elsewhere.
 */
export { auth, db, default } from './config/firebase.js';
