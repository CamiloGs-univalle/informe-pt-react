/**
 * MOVED to src/views/layout/Layout.jsx — see docs/ARCHITECTURE.md and
 * CHANGELOG.md. Kept as a re-export for backwards compatibility; safe to
 * delete once confirmed unused elsewhere.
 */
export { default } from '../views/layout/Layout.jsx';
