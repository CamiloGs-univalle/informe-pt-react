/**
 * src/logos.js — MOVED
 * ─────────────────────────────────────────────────────────────────────────
 * Relocated to `src/assets/logos.js` as part of the MVC architecture
 * migration (see CHANGELOG.md). Kept as a re-export for backwards
 * compatibility; safe to delete once you've confirmed nothing external
 * still imports from this path.
 */
export * from './assets/logos.js';
