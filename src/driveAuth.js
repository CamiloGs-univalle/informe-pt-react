/**
 * src/driveAuth.js — UNUSED / DEPRECATED
 * ─────────────────────────────────────────────────────────────────────────
 * This was an earlier, superseded Google Drive integration. It was never
 * imported by any component even before the MVC architecture migration
 * (the app uses `src/services/googleDrive.service.js`, formerly
 * `src/gdrive.js`, instead). Left in place only so the working tree still
 * matches version control 1:1; its original ~100 lines of dead code were
 * removed here. See CHANGELOG.md — safe to delete this file.
 */
