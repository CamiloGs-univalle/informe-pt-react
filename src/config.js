/**
 * src/config.js — MOVED
 * ─────────────────────────────────────────────────────────────────────────
 * Relocated to `src/config/app.config.js` (see docs/ARCHITECTURE.md and
 * CHANGELOG.md). Kept as a re-export for backwards compatibility; safe to
 * delete once confirmed unused elsewhere.
 */
export * from './config/app.config.js';
